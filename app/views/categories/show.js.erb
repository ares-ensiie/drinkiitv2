$('#meal-container').html("<%= j (render 'meals/meals') %>");
initMeals();
var containerY = $("#meal-container").offset().top - 100;
if (window.scrollY < containerY) {
	$('html, body').animate({
    	scrollTop: containerY
	}, 500);
}